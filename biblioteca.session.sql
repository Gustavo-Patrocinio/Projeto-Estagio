-- CREATE TABLE livros (
--   ID_LIVRO INT,
--   COD_BARRAS VARCHAR(20) PRIMARY KEY,
--   NOME_LIVRO VARCHAR(200),
--   CATEGORIA VARCHAR(200),
--   DISPONIBILIDADE BOOLEAN
-- );
-- CREATE TABLE pessoas (
--   ID_PESSOA SERIAL,
--   CPF VARCHAR(11) PRIMARY KEY,
--   STATUS_PESSOA VARCHAR(200),
--   NOME VARCHAR(100),
--   IDADE INT
-- );
-- CREATE TABLE emprestimos (
--   ID_EMPRESTIMO INT PRIMARY KEY,
--   CPF VARCHAR(11),
--   COD_BARRAS VARCHAR(20),
--   STATUS_EMPRESTIMO BOOLEAN,
--   DTA_INICIO_EMP TIMESTAMP,
--   DTA_FIM_EMP TIMESTAMP,
--   CONSTRAINT fk_pessoa FOREIGN KEY (CPF) REFERENCES pessoas(CPF),
--   CONSTRAINT fk_livro FOREIGN KEY (COD_BARRAS) REFERENCES livros(COD_BARRAS)
-- );
SELECT *
FROM pessoas;
-- DROP TABLE pessoas